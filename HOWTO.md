# HTML Deck 模板使用说明

复刻自肖涵（hanxiao.io）的纯 HTML 演示引擎：单文件、零依赖、固定画布、键盘/点击/hash 三路导航、浏览器直接导出 PDF。

## 快速开始

1. 复制 `deck.html` 到你的工作目录，双击用浏览器打开
2. 方向键/空格翻页，`f` 全屏，`#5` 形式的 URL 直达第 5 页
3. 用任意编辑器（VS Code 等）改内容，刷新浏览器即见

## 怎么加一页

在 `</div><!-- /stage -->` 之前复制任意一个 `<section class="slide">…</section>` 块，改内容。页码自动重排，无需手改。

## 页面类型对照

| 类型 | class | 用途 | 模板位置 |
|---|---|---|---|
| 标题页 | `.slide.title` | 开场：主标题/副题/姓名/二维码 | 第 1 页 |
| 卡片页 | `.slide`（内容通用） | 三卡片并列 + 底部结论条 | 第 2 页 |
| 大字主张页 | `.slide.claimslide` | 章节间金句 | 第 3 页 |
| 章节分隔页 | `.slide.divider` | 大编号 + 章节名 | 第 4 页 |
| 图表页 | `.slide` + `#c-xxx` 容器 | 左文右图，JS 画 SVG | 第 5 页 |
| 表格页 | `.slide` + `.dtable` | 方案/参数对比 | 第 6 页 |
| 二维码页 | `.slide` + `.qr-corner` | 放 repo/产品链接 | 第 7 页 |
| 结尾页 | `.slide.closing` | 收尾金句 + 链接 | 第 8 页 |

## 全局改哪里

| 要改什么 | 去哪改 |
|---|---|
| 主题色/圆角 | CSS 顶部 `:root{--cyan:…}` 变量，一处改全局生效 |
| 页眉页脚（全局） | HTML 里 `#chrome-src` 模板块 |
| 每页页眉标题 | 对应 `<section>` 的 `data-title` 属性 |
| 副标题英文 slogan | `#chrome-src` 里 `.en` / `.cn` |
| 图片/二维码 | 建 `img/` 目录放图，替换 `src` |
| 图表数据 | JS 里 `CHART_DATA`，新图在 `renderAll()` 挂载 |

## 布局速查

- `.body`：页面中部主区域（自动垂直居中），每页一个
- `.row`：横排容器；`.fill`：均分剩余宽度
- `.kicker`：页首小标签（自带渐变短线），放「EVIDENCE · 数据」这类导航提示
- `.takeaway`：底部居中结论条；`.cap`/`.lead`：正文段落
- 强调色 class：`.cy` 青 / `.bl` 蓝 / `.pk` 粉 / `.gr` 绿 / `.am` 琥珀
- 入场动画：给元素加 `au` class（可叠 `au-d1/d2/d3` 做依次浮现），打印自动禁用

## 画图表

无任何图表库，JS 里有一套 30 行 SVG 工具（`el`/`txt`/`mount`/`ylabel`），示例 `drawDemoChart()` 演示了柱状图完整画法：网格线 → 轴 → 柱 → 数值标签。散点/折线/哑铃图同套路，参照示例改即可。

大段数据建议学肖涵拆到独立 `data.js`（`window.A = {...}`），主文件里只留画法。

## 导出 PDF

浏览器 Ctrl+P → 目标「另存为 PDF」→ 纸张边距选「无」→ 勾选「背景图形」。每页自动一页纸，页眉页脚每页都在，动画自动定格为最终帧。

## 部署上网

整个目录推到任意静态托管即可（GitHub Pages / EdgeOne Pages / 公司服务器），链接后缀 `#页码` 可直达指定页，适合演讲后分享「看第 12 页」。

## 可选增强（deck.html 底部注释有现成代码）

- **数学公式**：本地放 MathJax `tex-svg.js`，两行接入
- **专业字体**：思源黑体/Inter 用 fontmin 子集化后 @font-face 自托管（肖涵的做法，页面秒开）
- **页内自动动画**：rAF 驱动，在 `show()` 里翻页时重启

## 工程约定建议

- 一个 talk 一个目录：`deck.html` + `img/` + `fonts/` + `data.js`
- 用 git 管理，改稿可 diff、可回滚
- 版本迭代靠 `deck.html?v=2` 查询参数破缓存（服务器端同理）
